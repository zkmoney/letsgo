Let's Go

$GOPATH
* Don't try to have multiple
* Do export it in your bash_profile

$GOBIN
* Let's you immediately run programs/commands you `go get` or `go install` (cool!)
* Also export it in your bash profile
* (hint: it's $GOPATH/bin)

